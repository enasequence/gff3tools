name: Validate before merge

on:
  pull_request:
    branches:
      - main

jobs:
  validate:
    name: Run Spotless + build +   Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Cache Gradle dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Run Spotless check
        run: ./gradlew spotlessCheck -Pgitlab_private_token=dummy-token

      - name: Run all tests
        run: ./gradlew test -Pgitlab_private_token=dummy-token
